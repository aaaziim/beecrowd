#include <stdio.h>

int main() {
    int A, B, SOMA;

    // Reading two integers
    scanf("%d %d", &A, &B);

    // Calculating the sum and storing it in SOMA
    SOMA = A + B;

    // Printing the result in the required format
    printf("SOMA = %d\n", SOMA);

    return 0;
}

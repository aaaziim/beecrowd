#include <stdio.h>

int main() {
    int A, B, X;

    // Reading two integers
    scanf("%d %d", &A, &B);

    // Calculating the sum and storing it in X
    X = A + B;

    // Printing the result in the required format
    printf("X = %d\n", X);

    return 0;
}

#include <stdio.h>

int main() {
    int A, B, PROD;

    // Reading two integers
    scanf("%d %d", &A, &B);

    // Calculating the product and storing it in PROD
    PROD = A * B;

    // Printing the result in the required format
    printf("PROD = %d\n", PROD);

    return 0;
}
